// Complete the function which converts a binary number (given as a string) to a decimal number.


function binToDec(bin){
    //the parseInt function is used to convert the binary number to decimal. The parseInt function parses a string argument and returns an integer based on the specified radix.
    return parseInt(bin, 2)
  }